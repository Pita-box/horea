# Deprecated Location

Project steering rules live in `RULES/CLAUDE.md` and canonical Kiro source lives in `.kiro/steering/CLAUDE.md`.

Codex command execution policy lives in `.codex/rules/default.rules`.

Do not maintain a separate copy here.
